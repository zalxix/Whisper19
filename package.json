{
  "name": "whisper-transcription-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve public",
    "build": "mkdir -p public && cp -r index.html app.js styles.css public/"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
